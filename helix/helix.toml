theme = "base16_terminal"

[keys.insert]
j = { k = "normal_mode" }
k = { j = "normal_mode" }

[keys.normal.space.t]
t = [
  ":sh RUSTFLAGS=-Awarnings cargo test -q -- --nocapture %sh{echo %{buffer_name} | sed -E 's|^src/||; s,/?(mod|main)\\.rs\\s*$,,; s|\\.rs$||; s|/|::|g' }",
]

